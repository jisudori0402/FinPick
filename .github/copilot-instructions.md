# FinPick workspace notes

- Django backend lives in backend/.
- Vue interface is rendered from backend/templates/index.html.
- SQLite database is used by default.
- Run migrations and seed data before starting the app.
